int @min_32(int a, int b) {
    return (a < b) ? a : b;
}
int @max_32(int a, int b) {
    return (a < b) ? b : a;
}
long @min_64(long a, long b) {
    return (a < b) ? a : b;
}
long @max_64(long a, long b) {
    return (a < b) ? b : a;
}
